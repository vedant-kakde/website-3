export type GetStartedIcon = {
  path: string;
  transform: number;
};

export type GetStartedItem = {
  title: string;
  description: string;
  buttonLabel: string;
  path: string;
  icon: GetStartedIcon;
};
